#include<bits/stdc++.h>

using namespace std;

int main(){
  int n1=0, n2=0;
  while (l1->next){
    n1 = n1 + (l1.value * 10);
    l1 = l1->next;
  }
  while (l2->next){
    n2 = n2 + (l2.value * 10);
    l2 = l2->next;
  }
  ListNode *sol = ListNode();
  sol
  return 0;
}