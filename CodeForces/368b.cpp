
/*
User: crox-x
LANG: C++                 
Time: 1681469000
*/
#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    cin >> n >> m;
    vector<int> num(n), mum(m);
    for (int i=0; i<n; i++) cin >> num[i];
    for (int i=0; i<m; i++) cin >> mum[i];
    for (int i=0; i<m; i++){
        
    }
    return 0;
}
    